```javascript
/*
  data - XML SVG text content
**/
function svgUtil(data) {
  /* 
    Implementation 
  */
  
  var result = {
    parsedOkay: Boolean,
    path: String,
    multiplePaths: Boolean,
    skippedTags: Array,
    skippedAttributes: Array,
    x: Number,
    y: Number,
    height: Number,
    width: Number,
  };

  return result;
}


```
